# Phase 2.1: Execution Plan

## Modify `proxy/src/index.ts`
Add a dedicated logic branch to match the string `/delegate`.

```typescript
// Detect Delegation
const delegateMatch = payload.comment?.body?.match(/\/delegate\s+([\w-]+)/);

if (delegateMatch && isAuthorized) {
  const agentName = delegateMatch[1]; // e.g. 'frontend-specialist'
  
  // Apply Label to GitHub Issue
  const issueUrl = payload.issue ? payload.issue.url : payload.pull_request.issue_url; // Safely grab issue anchor
  
  await fetch(`${issueUrl}/labels`, {
    method: 'POST',
    headers: {
      'Authorization': `token ${env.GITHUB_TOKEN}`,
      'User-Agent': 'Cloudflare-Worker',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      labels: [`agent:${agentName}`]
    })
  });

  // Notify Discord Context
  ctx.waitUntil(sendNotification(env, `[DELEGATION] Jules delegated ${type} #${number} to \`agent:${agentName}\``));

  // Skip executing the actual Jules API LLM call!
  return new Response(`Delegated to ${agentName}`, { status: 202 });
}
```

## Update Documentation
- Ensure `docs/CONFIGURATION.md` is updated with the new `/delegate [agent-name]` definition.

## Validation
- `wrangler run deploy` to update the proxy logic.
